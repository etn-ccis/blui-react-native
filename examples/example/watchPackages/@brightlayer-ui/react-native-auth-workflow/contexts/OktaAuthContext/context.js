/**
 * @packageDocumentation
 * @module OktaAuthContext
 */
import { createContext } from 'react';
/**
 * Okta Auth Context is used to access context in the okta authentication workflow
 */
export const OktaAuthContext = createContext(null);
