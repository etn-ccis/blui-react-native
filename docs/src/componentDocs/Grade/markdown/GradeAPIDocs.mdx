import Box from '@mui/material/Box';
import GradeComponentImage from '../images/grade.png';
import GradeCSubComponentImage from '../images/gradesSubcomponent.png';

# Grade

The Grade component is lightweight wrapper around the React Native Paper [Avatar](https://callstack.github.io/react-native-paper/4.0/avatar-text.html) that represents a rating. It shows a specified rating along with a color-coded background color based on that rating.

{/* TODO: This image needs to be updated to show standard Grades, not random colors */}

<Box
  sx={{
    width: '100%',
    textAlign: 'center',
  }}>
  <Box
    sx={{
      width: '100%',
      maxWidth: '150px',
    }}
    component="img"
    alt="Gradecomponent"
    src={GradeComponentImage}
  />
</Box>

<Box
  sx={{
    width: '100%',
    textAlign: 'center',
  }}>
  ![placeholder image](https://placehold.co/400x300)
</Box>

## Grade API

<Box>

| Prop Name       | Description                        | Type                          | Required | Default                  |
| --------------- | ---------------------------------- | ----------------------------- | -------- | ------------------------ |
| label           | The text shown in the circle       | `string`                      | yes      |                          |
| fontColor       | The color of the text label        | `string`                      | no       | `theme.colors.onPrimary` |
| backgroundColor | The background color of the circle | `string`                      | no       | `theme.colors.primary`   |
| size            | The size of the circle in px       | `number`                      | no       | 40                       |
| theme           | Theme value overrides              | `$DeepPartial<ExtendedTheme>` | no       |                          |

</Box>

Any other props will be provided to the root element ([**View**](https://reactnative.dev/docs/view)).

## Grade Presets

There are a number of preset values available with standard grades and colors already configured. You should use these if they make sense for your target location (these are based on the US grade system). Presets are available by accessing the nested component by name (e.g., `<Grade.APlus/>`)

<Box
  sx={{
    width: '100%',
    textAlign: 'center',
  }}>
  <Box
    sx={{
      width: '100%',
      maxWidth: '200px',
    }}
    component="img"
    alt="Grade Subcomponent"
    src={GradeCSubComponentImage}
  />
</Box>

### Grade Presets API

All Grade Presets have the following Props

<Box>

| Prop Name | Description         | Type     | Required | Default |
| --------- | ------------------- | -------- | -------- | ------- |
| size      | The radius of Grade | `number` | No       | 40      |

</Box>

### Styles

You can override the internal style used by Brightlayer UI by passing a `style` prop.
