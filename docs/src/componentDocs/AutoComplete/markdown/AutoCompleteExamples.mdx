import { AutoComplete } from '../examples/AutoComplete';
import { AutoCompleteCustomTags } from '../examples/AutoCompleteCustomTags';
import { AutoCompleteLimitTags } from '../examples/AutoCompleteLimitTags';

## Auto Complete

The `<AutoComplete>` is a component that features a standard text input accompanied by a list of suggested options.

<AutoComplete />

The `<AutoComplete>` component with `allowCustomtag` prop to pass text to the chip.

<AutoCompleteCustomTags />

The `<AutoComplete>` component with `limitTags` prop to set the number of Chips to be shown.

<AutoCompleteLimitTags />

