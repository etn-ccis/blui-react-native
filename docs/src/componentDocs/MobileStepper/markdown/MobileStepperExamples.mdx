import {MobileStepperDotVariant} from '../examples/MobileStepperDotVariant';
import {MobileStepperTextVariant} from '../examples/MobileStepperTextVariant';
import {MobileStepperProgressVariant} from '../examples/MobileStepperProgressVariant';

## Mobile Stepper

The basic mobile stepper uses dots to indicate progress.

<MobileStepperDotVariant />

## Using Text

If you have more than a few steps or limited space in your container, you can use a text label to indicate the current step.

<MobileStepperTextVariant />

## Progress Bar

If you have more than a few steps or limited space in your container, you can use a progress bar to indicate the current step.

<MobileStepperProgressVariant />
